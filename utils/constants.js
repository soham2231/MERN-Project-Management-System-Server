const PROJECT_STATUS = ["Pending", "In Progress", "Completed"];

const TASK_STATUS = ["Pending", "In Progress", "Completed"];

const TASK_PRIORITY = ["Low", "Medium", "High"];

const USER_ROLES = ["Admin", "HOD", "Member"];

module.exports = {
  PROJECT_STATUS,
  TASK_STATUS,
  TASK_PRIORITY,
  USER_ROLES,
};
